<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Дозвіл на доступ до мережі (надається автоматично, без запиту користувачу) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Bluetooth: Android 12+ (API 31+) — окремі дозволи на пошук і підключення -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Bluetooth: Android 11 і старіші -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Геолокація: приблизна (мережа) і точна (GPS). На Android 11- точна потрібна також для BLE -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Сповіщення: на Android 13+ (API 33) це runtime-дозвіл, його треба запитати -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Камера: дозвіл на зйомку. required="false" — камера не обов'язкова для встановлення -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- required="false": застосунок можна встановити й на пристрій без BLE -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <application
        android:name=".SmartLifeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Приймач натискання кнопки «Випив склянку» у сповіщенні -->
        <receiver
            android:name=".notifications.WaterActionReceiver"
            android:exported="false" />

        <!-- FileProvider дає камері тимчасовий доступ на запис до нашого файлу -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
